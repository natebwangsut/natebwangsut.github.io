---
import { Icon } from "astro-icon/components";
import Content from "./Content.astro";

interface Props {
  title?: string;
  titleClass?: string;
  body?: string;
  bodyClass?: string;
  colSpan?: string;
  rowSpan?: string;
  href?: string;
  colorText?: string;
}

const {
  title,
  titleClass,
  body,
  bodyClass,
  colSpan,
  rowSpan,
  href,
  colorText,
} = Astro.props;
---

<div
  class={`card group overflow-hidden translate-y-[-40%] bg-gradient-to-b from-darkslate-500 to-darkslate-700 shadow-lg rounded-lg p-6 border border-darkslate-100 hover:border-primary-600 hover:shadow-[0_10px_40px_-16px_rgba(2,199,156,0.45)] align-start flex-none justify-start relative transform perspective-1200 w-full transition-[border-color,box-shadow] duration-300 ease-out col-span-1 h-auto ${
    colSpan || "md:col-span-2"
  } ${rowSpan || ""}`}
>
  {
    href ? (
      <a href={href} class={`h-full w-full ${colorText || " "}`}>
        <Icon
          name="ri:arrow-right-up-line"
          class="h-6 float-right group-hover:text-primary-500 group-hover:translate-x-1 group-hover:-translate-y-1 transition-transform ease-in-out duration-100 z-20"
        />
        <Content
          title={title}
          titleClass={`${titleClass}`}
          body={body}
          bodyClass={bodyClass}
        >
          <slot />
        </Content>
      </a>
    ) : (
      <Content
        title={title}
        titleClass={titleClass}
        body={body}
        bodyClass={bodyClass}
      >
        <slot />
      </Content>
    )
  }
</div>
